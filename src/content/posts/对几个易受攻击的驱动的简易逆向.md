---
title: 对几个易受攻击的驱动的简易逆向
published: 2026-06-12
description: '记录 ipctype.sys、portwell.sys、ppa_x64.sys、SysInfoX64.sys 的入口、IOCTL 分发和主要攻击面。'
image: ''
tags: [Windows, Driver, Reverse]
category: 'Reverse'
draft: false 
lang: 'zh_CN'
---
相关驱动文件可在 [此仓库](https://github.com/lallouslab/vulnerable-drivers-ex) 找到。

使用工具: `IDA PRO 9.2sp1`

相关驱动均已在`Win11 24H2`上测试完毕

本文的取舍如下：第一个驱动保留较完整的 IRP 和 IOCTL 基础说明，后续驱动只记录样本自身的差异、关键代码和攻击面。较长的反编译代码块用于保留分析现场；阅读时先看每节末尾的表格，再按需回看代码。

整体速览：

| 驱动 | 设备名 | 主要能力 |
|---|---|---|
| `ipctype.sys` | `\\Device\\IPCType` | 映射指定物理地址到用户态。 |
| `portwell.sys` | `\\Device\\PORTWELL_0_1` | MSR、端口 I/O、物理内存、PCI 配置空间访问。 |
| `ppa_x64.sys` | `\\Device\\PhyMem` | 物理内存映射、端口 I/O、PCI 配置空间访问。 |
| `SysInfoX64.sys` | `\\Device\\CrystalSysInfo` | MSR、端口 I/O、物理内存、PCI 配置空间、中断连接。 |

**第一个：[ipctype.sys](https://github.com/lallouslab/vulnerable-drivers-ex/blob/main/drivers/ipctype.sys)**：
DriverEntry 反编译结果：

```c title="ipctype.sys: DriverEntry"
NTSTATUS __stdcall DriverEntry(_DRIVER_OBJECT *DriverObject, PUNICODE_STRING RegistryPath)
{
  int v3; // ebx
  struct _UNICODE_STRING DeviceName; // [rsp+40h] [rbp-28h] BYREF
  struct _UNICODE_STRING DestinationString; // [rsp+50h] [rbp-18h] BYREF
  PDEVICE_OBJECT DeviceObject; // [rsp+80h] [rbp+18h] BYREF

  DeviceObject = 0;
  RtlInitUnicodeString(&DeviceName, L"\\Device\\IPCType");
  v3 = IoCreateDevice(DriverObject, 0, &DeviceName, 0x8010u, 0, 1u, &DeviceObject);
  if ( v3 >= 0 )
  {
    DriverObject->MajorFunction[14] = (PDRIVER_DISPATCH)&sub_110D0;
    DriverObject->MajorFunction[2] = (PDRIVER_DISPATCH)&sub_110D0;
    DriverObject->MajorFunction[0] = (PDRIVER_DISPATCH)&sub_110D0;
    DriverObject->DriverUnload = (PDRIVER_UNLOAD)sub_11230;
    RtlInitUnicodeString(&DestinationString, L"\\DosDevices\\IPCType");
    v3 = IoCreateSymbolicLink(&DestinationString, &DeviceName);
    if ( v3 < 0 )
      IoDeleteDevice(DeviceObject);
  }
  return v3;
}
```

这里的 `DriverObject->MajorFunction` 是驱动对象中的派发表，数组下标对应 IRP major function code。IDA 这里用十进制显示数组下标，因此：

| 下标 | 十六进制 | IRP major function | 含义 |
|---:|---:|---|---|
| `14` | `0x0E` | `IRP_MJ_DEVICE_CONTROL` | 处理用户态 `DeviceIoControl` 或上层驱动发来的 IOCTL 请求。 |
| `2` | `0x02` | `IRP_MJ_CLOSE` | 处理目标设备对象关联的文件对象最后一个句柄被关闭后的关闭请求。 |
| `0` | `0x00` | `IRP_MJ_CREATE` | 处理打开文件对象或设备对象句柄的请求。 |

也就是说，这个驱动把 `IRP_MJ_CREATE`、`IRP_MJ_CLOSE` 和 `IRP_MJ_DEVICE_CONTROL` 都交给同一个分发函数 `sub_110D0` 处理。其中最值得继续跟进的是 `IRP_MJ_DEVICE_CONTROL`，这个样本的敏感行为也落在 IOCTL 分发逻辑里。

继续逆向 `sub_110D0`：

```c title="ipctype.sys: sub_110D0"
__int64 __fastcall sub_110D0(__int64 a1, IRP *Irp)
{
  struct _IO_STACK_LOCATION *CurrentStackLocation; // rax
  struct _IRP *MasterIrp; // rsi
  ULONG Options; // ebp
  ULONG Length; // ebx
  DWORD LowPart; // eax
  __int64 MdlAddress_high; // rcx
  int Status_1; // eax
  __int64 MdlAddress_low; // rcx
  __int64 v11; // rdx
  __int64 v12; // rdx
  __int64 MdlAddress_low_1; // rcx
  unsigned int Status; // ebx
  __int64 v16; // [rsp+38h] [rbp+10h] BYREF

  CurrentStackLocation = Irp->Tail.Overlay.CurrentStackLocation;
  Irp->IoStatus.Status = 0;
  Irp->IoStatus.Information = 0;
  MasterIrp = Irp->AssociatedIrp.MasterIrp;
  Options = CurrentStackLocation->Parameters.Create.Options;
  Length = CurrentStackLocation->Parameters.Read.Length;
  if ( CurrentStackLocation->MajorFunction != 14 )
    goto LABEL_26;
  LowPart = CurrentStackLocation->Parameters.Read.ByteOffset.LowPart;
  if ( LowPart == -2146426816 )
  {
    if ( IoIs32bitProcess(Irp) )
    {
      if ( Options == 16 && Length == 16 )
      {
        MdlAddress_low = SLODWORD(MasterIrp->MdlAddress);
        v11 = *(unsigned int *)(&MasterIrp->Size + 1);
        v16 = 0;
        Status_1 = sub_11270(MdlAddress_low, v11, &v16);
        if ( Status_1 >= 0 )
        {
          HIDWORD(MasterIrp->MdlAddress) = v16;
          Irp->IoStatus.Information = 16;
        }
        goto LABEL_24;
      }
    }
    else if ( Options == 28 && Length == 28 )
    {
      v12 = LODWORD(MasterIrp->MdlAddress);
      MdlAddress_low_1 = *(__int64 *)((char *)&MasterIrp->MdlAddress + 4);
      v16 = 0;
      Status_1 = sub_11270(MdlAddress_low_1, v12, &v16);
      if ( Status_1 >= 0 )
      {
        *(_QWORD *)(&MasterIrp->Flags + 1) = v16;
        Irp->IoStatus.Information = 28;
      }
      goto LABEL_24;
    }
    goto LABEL_25;
  }
  if ( LowPart == -2146426812 )
  {
    if ( IoIs32bitProcess(Irp) )
    {
      if ( Options == 16 && !Length )
      {
        MdlAddress_high = SHIDWORD(MasterIrp->MdlAddress);
LABEL_11:
        Status_1 = sub_113B0(MdlAddress_high);
LABEL_24:
        Irp->IoStatus.Status = Status_1;
        goto LABEL_26;
      }
    }
    else if ( Options == 28 && !Length )
    {
      MdlAddress_high = *(_QWORD *)(&MasterIrp->Flags + 1);
      goto LABEL_11;
    }
LABEL_25:
    Irp->IoStatus.Status = -1073741811;
    goto LABEL_26;
  }
  if ( LowPart != -2146426808 && LowPart != -2146426804 )
    Irp->IoStatus.Status = -1073741811;
LABEL_26:
  Status = Irp->IoStatus.Status;
  IofCompleteRequest(Irp, 0);
  return Status;
}
```

到这里可以确认，第二个参数就是传入进来的 IRP 值。这个函数先拿到 `Irp->Tail.Overlay.CurrentStackLocation`，再通过 `CurrentStackLocation->MajorFunction` 判断 IRP major function code；当值为 `14`，也就是 `IRP_MJ_DEVICE_CONTROL` 时，继续读取当前 I/O 栈里的 IOCTL 参数。

| 汇编偏移 | IDA 当前显示 | `Parameters.DeviceIoControl` 字段 | 用途 |
|---:|---|---|---|
| `+0x08` | `Parameters.Read.Length` | `OutputBufferLength` | 检查输出缓冲区长度。 |
| `+0x10` | `Parameters.Create.Options` | `InputBufferLength` | 检查输入缓冲区长度。 |
| `+0x18` | `Parameters.Read.ByteOffset.LowPart` | `IoControlCode` | 判断 IOCTL 控制码。 |

`sub_110D0` 中出现了四个 IOCTL 控制码，按 `CTL_CODE(DeviceType, Function, Method, Access)` 拆开如下：

| IOCTL | 有符号十进制显示 | DeviceType | Function | Method | Access | 行为 |
|---:|---:|---:|---:|---:|---:|---|
| `0x80102040` | `-2146426816` | `0x8010` | `0x810` | `METHOD_BUFFERED` | `FILE_ANY_ACCESS` | 调用 `sub_11270` 建立映射。 |
| `0x80102044` | `-2146426812` | `0x8010` | `0x811` | `METHOD_BUFFERED` | `FILE_ANY_ACCESS` | 调用 `sub_113B0` 释放映射。 |
| `0x80102048` | `-2146426808` | `0x8010` | `0x812` | `METHOD_BUFFERED` | `FILE_ANY_ACCESS` | 返回成功，`IoStatus.Information` 保持为 `0`。 |
| `0x8010204C` | `-2146426804` | `0x8010` | `0x813` | `METHOD_BUFFERED` | `FILE_ANY_ACCESS` | 返回成功，`IoStatus.Information` 保持为 `0`。 |

因为四个 IOCTL 的 `Method` 都是 `0`，也就是 `METHOD_BUFFERED`，所以用户态传入的缓冲区会被 I/O 管理器放到 `Irp->AssociatedIrp.SystemBuffer`。汇编里 `mov rsi, [rdx+18h]` 对应的就是这个缓冲区地址。

`0x80102040` 会根据调用进程是否为 32 位进程走两套长度检查和字段偏移：

| 进程类型 | `InputBufferLength` | `OutputBufferLength` | 访问偏移 | 行为 |
|---|---:|---:|---|---|
| 32 位进程 | `0x10` | `0x10` | 读取 `[SystemBuffer+0x04]` | 作为 `NumberOfBytes` 传入 `sub_11270`。 |
| 32 位进程 | `0x10` | `0x10` | 读取 `[SystemBuffer+0x08]` | 使用 `movsxd` 取出 32 位物理地址并符号扩展后传入 `sub_11270`。 |
| 32 位进程 | `0x10` | `0x10` | 写入 `[SystemBuffer+0x0C]` | 写回 `sub_11270` 返回的映射地址低 32 位。 |
| 64 位进程 | `0x1C` | `0x1C` | 读取 `[SystemBuffer+0x08]` | 作为 `NumberOfBytes` 传入 `sub_11270`。 |
| 64 位进程 | `0x1C` | `0x1C` | 读取 `[SystemBuffer+0x0C]` | 取出 64 位物理地址后传入 `sub_11270`。 |
| 64 位进程 | `0x1C` | `0x1C` | 写入 `[SystemBuffer+0x14]` | 写回 `sub_11270` 返回的 64 位映射地址。 |

`0x80102044` 是对应的释放接口：

| 进程类型 | `InputBufferLength` | `OutputBufferLength` | 访问偏移 | 行为 |
|---|---:|---:|---|---|
| 32 位进程 | `0x00` | `0x10` | 读取 `[SystemBuffer+0x0C]` | 作为参数传入 `sub_113B0`。 |
| 64 位进程 | `0x00` | `0x1C` | 读取 `[SystemBuffer+0x14]` | 作为参数传入 `sub_113B0`。 |

长度检查不满足时，`sub_110D0` 会把 `Irp->IoStatus.Status` 设置为 `0xC000000D`，也就是 `STATUS_INVALID_PARAMETER`。未知 IOCTL 也走同一个状态码。处理结束后统一调用 `IofCompleteRequest(Irp, 0)` 完成 IRP。

继续看 `0x80102040` 调用的 `sub_11270`：

```c title="ipctype.sys: sub_11270"
__int64 __fastcall sub_11270(PHYSICAL_ADDRESS MdlAddress_low, unsigned int NumberOfBytes, _QWORD *a3)
{
  SIZE_T Length; // r12
  unsigned int v5; // ebx
  PVOID VirtualAddress; // rax
  void *BaseAddress; // rsi
  struct _MDL *Mdl; // rax
  struct _MDL *Mdl_1; // r13
  PVOID v10; // rdi
  _QWORD *PoolWithTag; // rax

  Length = NumberOfBytes;
  v5 = 0;
  VirtualAddress = MmMapIoSpace(MdlAddress_low, NumberOfBytes, MmNonCached);
  BaseAddress = VirtualAddress;
  if ( VirtualAddress )
  {
    Mdl = IoAllocateMdl(VirtualAddress, Length, 0, 0, 0);
    Mdl_1 = Mdl;
    if ( Mdl )
    {
      MmBuildMdlForNonPagedPool(Mdl);
      v10 = MmMapLockedPagesSpecifyCache(Mdl_1, 1, MmNonCached, 0, 0, 0x10u);
      if ( v10 )
      {
        PoolWithTag = ExAllocatePoolWithTag(NonPagedPool, 0x28u, 0x54435049u);
        PoolWithTag[1] = Mdl_1;
        PoolWithTag[2] = BaseAddress;
        PoolWithTag[3] = v10;
        *((_DWORD *)PoolWithTag + 8) = Length;
        *PoolWithTag = P;
        P = PoolWithTag;
        *a3 = v10;
      }
      else
      {
        MmUnmapIoSpace(BaseAddress, (unsigned int)Length);
        return (unsigned int)-1073741670;
      }
    }
    else
    {
      MmUnmapIoSpace(BaseAddress, Length);
      return (unsigned int)-1073741670;
    }
  }
  else
  {
    return (unsigned int)-1073741670;
  }
  return v5;
}
```

这段逻辑的执行顺序非常直接：

1. 使用用户提供的 `PhysicalAddress` 和 `NumberOfBytes` 调用 `MmMapIoSpace`，把物理地址范围映射到内核虚拟地址。
2. 对 `MmMapIoSpace` 返回的 `BaseAddress` 调用 `IoAllocateMdl`。
3. 调用 `MmBuildMdlForNonPagedPool` 初始化 MDL。
4. 调用 `MmMapLockedPagesSpecifyCache`，并且第二个参数传入 `UserMode`，把同一段页映射到用户态地址空间。
5. 使用 `ExAllocatePoolWithTag(NonPagedPool, 0x28, 0x54435049)` 分配一条记录，插入全局链表 `P`。
6. 通过第三个参数把用户态映射地址写回给调用方。

全局链表 `P` 的每条记录大小为 `0x28`，字段由写入位置确认如下：

| 偏移 | 内容 | 写入位置 |
|---:|---|---|
| `0x00` | 下一条记录指针 | `*PoolWithTag = P` |
| `0x08` | `Mdl` | `PoolWithTag[1] = Mdl` |
| `0x10` | `BaseAddress` | `PoolWithTag[2] = BaseAddress` |
| `0x18` | `UserAddress` | `PoolWithTag[3] = UserAddress` |
| `0x20` | `Length` | `*((_DWORD *)PoolWithTag + 8) = Length` |

这里的核心问题是：调用方可以控制传入 `MmMapIoSpace` 的物理地址和长度，驱动随后又把映射结果通过 `MmMapLockedPagesSpecifyCache(..., UserMode, ...)` 暴露到用户态。驱动没有对物理地址范围做限制，也没有检查调用方权限；因此用户态可以借这个 IOCTL 映射任意物理地址范围。

再看 `0x80102044` 调用的 `sub_113B0`：

```c title="ipctype.sys: sub_113B0"
__int64 __fastcall sub_113B0(PVOID MdlAddress_high)
{
  PVOID *P; // rbx
  PVOID *P_1; // rdi

  P = (PVOID *)P;
  P_1 = (PVOID *)P;
  while ( P )
  {
    if ( P[3] == MdlAddress_high )
    {
      MmUnmapLockedPages(P[3], (PMDL)P[1]);
      IoFreeMdl((PMDL)P[1]);
      MmUnmapIoSpace(P[2], *((unsigned int *)P + 8));
      if ( P == P )
        P = *P;
      else
        *P_1 = *P;
      ExFreePool(P);
      return 0;
    }
    P_1 = P;
    P = (PVOID *)*P;
  }
  return 0;
}
```

IDA 的反编译输出里，`sub_113B0` 的局部变量名与全局链表头 `P` 同名；链表更新语义以汇编里的全局写入为准，涉及 `0x11401` 处读取全局 `P`、`0x1140A` 处写回全局 `P`。

这个函数遍历全局链表 `P`，查找偏移 `0x18` 保存的用户态映射地址是否等于传入参数。找到后依次执行：

1. `MmUnmapLockedPages(Current[3], (PMDL)Current[1])`
2. `IoFreeMdl((PMDL)Current[1])`
3. `MmUnmapIoSpace(Current[2], *((unsigned int *)Current + 8))`
4. 从链表 `P` 中摘除当前记录
5. `ExFreePool(Current)`

如果没有找到匹配记录，函数仍然返回 `0`。这一点会让调用方无法通过返回值区分“释放成功”和“没有找到记录”。

最后整理一下 `ipctype.sys` 的攻击面：

| 位置 | 结论 |
|---|---|
| 设备名 | `\\Device\\IPCType` |
| 符号链接 | `\\DosDevices\\IPCType` |
| 设备类型 | `0x8010` |
| 关键 IOCTL | `0x80102040`、`0x80102044` |
| 映射函数 | `sub_11270` |
| 释放函数 | `sub_113B0` |
| 全局链表头 | `P`，地址 `0x13110` |
| 主要风险 | 用户态可控制物理地址和长度，并拿到用户态映射地址。 |
| 额外风险 | `ExAllocatePoolWithTag` 返回值未判空；全局链表 `P` 的增删路径没有同步保护。 |

**第二个：[portwell.sys](https://github.com/lallouslab/vulnerable-drivers-ex/blob/main/drivers/portwell.sys)**：
DriverEntry 反编译结果：

```c title="portwell.sys: DriverEntry"
NTSTATUS __stdcall DriverEntry(_DRIVER_OBJECT *DriverObject, PUNICODE_STRING RegistryPath)
{
  struct _UNICODE_STRING SymbolicLinkName; // [rsp+40h] [rbp-38h] BYREF
  PDEVICE_OBJECT DeviceObject; // [rsp+50h] [rbp-28h] BYREF
  struct _UNICODE_STRING DestinationString; // [rsp+58h] [rbp-20h] BYREF
  int v6; // [rsp+68h] [rbp-10h]

  DeviceObject = 0;
  RtlInitUnicodeString(&DestinationString, L"\\Device\\PORTWELL_0_1");
  v6 = IoCreateDevice(DriverObject, 0, &DestinationString, 0xEA60u, 0x100u, 0, &DeviceObject);
  if ( v6 >= 0 )
  {
    dword_14110 = 0;
    DriverObject->MajorFunction[14] = (PDRIVER_DISPATCH)sub_11430;
    DriverObject->MajorFunction[0] = (PDRIVER_DISPATCH)sub_11430;
    DriverObject->MajorFunction[2] = (PDRIVER_DISPATCH)sub_11430;
    DriverObject->DriverUnload = (PDRIVER_UNLOAD)sub_113B0;
    RtlInitUnicodeString(&SymbolicLinkName, L"\\DosDevices\\PORTWELL_0_1");
    v6 = IoCreateSymbolicLink(&SymbolicLinkName, &DestinationString);
    if ( v6 < 0 )
      IoDeleteDevice(DeviceObject);
    return v6;
  }
  else
  {
    dword_14110 = -1;
    return v6;
  }
}
```

继续逆向 `sub_11430`：

```c title="portwell.sys: sub_11430"
__int64 __fastcall sub_11430(__int64 a1, IRP *Irp)
{
  __int64 v3; // [rsp+30h] [rbp-28h]
  unsigned int Status; // [rsp+3Ch] [rbp-1Ch]
  char n2; // [rsp+40h] [rbp-18h]
  unsigned int v6; // [rsp+44h] [rbp-14h]

  Irp->IoStatus.Information = 0;
  v3 = sub_11220(Irp);
  Status = -1073741822;
  n2 = *(_BYTE *)v3;
  if ( !*(_BYTE *)v3 )
  {
    if ( dword_14110 != -1 )
      ++dword_14110;
    Status = 0;
    goto LABEL_47;
  }
  if ( n2 == 2 )
  {
    if ( dword_14110 != -1 )
      --dword_14110;
    Status = 0;
    goto LABEL_47;
  }
  if ( n2 != 14 )
    goto LABEL_47;
  v6 = *(_DWORD *)(v3 + 24);
  if ( v6 > 0xEA606440 )
  {
    if ( v6 > 0xEA60A448 )
    {
      if ( v6 != -362765236 )
      {
        if ( v6 == -362765228 )
        {
          Status = sub_11F80(
                     Irp->AssociatedIrp.MasterIrp,
                     *(_DWORD *)(v3 + 16),
                     Irp->AssociatedIrp.MasterIrp,
                     *(_DWORD *)(v3 + 8),
                     (__int64)&Irp->IoStatus.Information);
        }
        else if ( v6 == -362765220 )
        {
          Status = sub_11C30(
                     Irp->AssociatedIrp.MasterIrp,
                     *(_DWORD *)(v3 + 16),
                     Irp->AssociatedIrp.MasterIrp,
                     *(_DWORD *)(v3 + 8),
                     (__int64)&Irp->IoStatus.Information);
        }
        goto LABEL_47;
      }
    }
    else if ( v6 != -362765240 )
    {
      if ( v6 == -362781616 )
      {
        Status = sub_11E40(
                   Irp->AssociatedIrp.MasterIrp,
                   *(_DWORD *)(v3 + 16),
                   Irp->AssociatedIrp.MasterIrp,
                   *(_DWORD *)(v3 + 8),
                   (__int64)&Irp->IoStatus.Information);
        goto LABEL_47;
      }
      if ( v6 == -362781608 )
      {
        Status = sub_11BA0(
                   Irp->AssociatedIrp.MasterIrp,
                   *(_DWORD *)(v3 + 16),
                   Irp->AssociatedIrp.MasterIrp,
                   *(_DWORD *)(v3 + 8),
                   (__int64)&Irp->IoStatus.Information);
        goto LABEL_47;
      }
      if ( v6 != -362765284 && v6 != -362765244 )
        goto LABEL_47;
    }
    Status = sub_119A0(
               *(unsigned int *)(v3 + 24),
               Irp->AssociatedIrp.MasterIrp,
               *(unsigned int *)(v3 + 16),
               Irp->AssociatedIrp.MasterIrp,
               *(_DWORD *)(v3 + 8),
               &Irp->IoStatus.Information);
    goto LABEL_47;
  }
  if ( v6 == -362781632 )
    goto LABEL_29;
  if ( v6 <= 0xEA602410 )
  {
    switch ( v6 )
    {
      case 0xEA602410:
        Status = sub_11B30(
                   Irp->AssociatedIrp.MasterIrp,
                   *(_DWORD *)(v3 + 16),
                   Irp->AssociatedIrp.MasterIrp,
                   *(_DWORD *)(v3 + 8),
                   (__int64)&Irp->IoStatus.Information);
        break;
      case 0xEA602400:
        *(_DWORD *)Irp->AssociatedIrp.MasterIrp = 0x10000;
        Irp->IoStatus.Information = 4;
        Status = 0;
        break;
      case 0xEA602404:
        *(_DWORD *)Irp->AssociatedIrp.MasterIrp = dword_14110;
        Irp->IoStatus.Information = 4;
        Status = 0;
        break;
      case 0xEA602408:
        Status = sub_11A50(
                   Irp->AssociatedIrp.MasterIrp,
                   *(_DWORD *)(v3 + 16),
                   Irp->AssociatedIrp.MasterIrp,
                   *(_DWORD *)(v3 + 8),
                   (__int64)&Irp->IoStatus.Information);
        break;
      case 0xEA60240C:
        Status = sub_11AC0(
                   Irp->AssociatedIrp.MasterIrp,
                   *(_DWORD *)(v3 + 16),
                   Irp->AssociatedIrp.MasterIrp,
                   *(_DWORD *)(v3 + 8),
                   (__int64)&Irp->IoStatus.Information);
        break;
    }
    goto LABEL_47;
  }
  if ( v6 == -362798060 )
    __halt();
  if ( v6 == -362781672 || v6 == -362781664 || v6 == -362781660 )
LABEL_29:
    Status = sub_118E0(
               *(_DWORD *)(v3 + 24),
               Irp->AssociatedIrp.MasterIrp,
               *(_DWORD *)(v3 + 16),
               Irp->AssociatedIrp.MasterIrp,
               *(_DWORD *)(v3 + 8),
               (__int64)&Irp->IoStatus.Information);
LABEL_47:
  Irp->IoStatus.Status = Status;
  IofCompleteRequest(Irp, 0);
  return Status;
}
```

整理如下，只写这个驱动自身的关键信息。

| 位置 | 值 |
|---|---|
| 设备名 | `\\Device\\PORTWELL_0_1` |
| 符号链接 | `\\DosDevices\\PORTWELL_0_1` |
| 设备类型 | `0xEA60` |
| 卸载函数 | `sub_113B0` |
| 全局计数 | `dword_14110`，地址 `0x14110` |

`sub_11430` 对 `IRP_MJ_CREATE` 和 `IRP_MJ_CLOSE` 只维护 `dword_14110`。`IRP_MJ_DEVICE_CONTROL` 通过 `IoControlCode` 分发，缓冲区来自 `Irp->AssociatedIrp.MasterIrp`，也就是 `SystemBuffer`。

| IOCTL | Function | handler | 行为 |
|---:|---:|---|---|
| `0xEA602400` | `0x900` | `sub_11430` 内联处理 | 写回 `0x10000`。 |
| `0xEA602404` | `0x901` | `sub_11430` 内联处理 | 写回 `dword_14110`。 |
| `0xEA602408` | `0x902` | `sub_11A50` | `rdmsr`。 |
| `0xEA60240C` | `0x903` | `sub_11AC0` | `wrmsr`。 |
| `0xEA602410` | `0x904` | `sub_11B30` | `rdpmc`。 |
| `0xEA602414` | `0x905` | `sub_11430` 内联处理 | `hlt`。 |
| `0xEA606420` | `0x908` | `sub_118E0` | `in al, dx`，读 1 字节端口。 |
| `0xEA606424` | `0x909` | `sub_118E0` | `in ax, dx`，读 2 字节端口。 |
| `0xEA606440` | `0x910` | `sub_118E0` | `in eax, dx`，读 4 字节端口。 |
| `0xEA606450` | `0x914` | `sub_11E40` | `MmMapIoSpace` 后读物理地址内容。 |
| `0xEA606458` | `0x916` | `sub_11BA0` | `HalGetBusDataByOffset` 读 PCI 配置空间。 |
| `0xEA60A444` | `0x911` | `sub_119A0` | `out dx, al`，写 1 字节端口。 |
| `0xEA60A448` | `0x912` | `sub_119A0` | `out dx, ax`，写 2 字节端口。 |
| `0xEA60A44C` | `0x913` | `sub_119A0` | `out dx, eax`，写 4 字节端口。 |
| `0xEA60A454` | `0x915` | `sub_11F80` | `MmMapIoSpace` 后写物理地址内容。 |
| `0xEA60A45C` | `0x917` | `sub_11C30` | `HalSetBusDataByOffset` 写 PCI 配置空间。 |

几个关键缓冲区格式：

| handler | 输入要求 | 关键字段 |
|---|---|---|
| `sub_11A50` | `SystemBuffer+0x00` 为 MSR 编号 | 读取结果写回 `SystemBuffer+0x00`，`Information = 8`。 |
| `sub_11AC0` | `SystemBuffer+0x00` 为 MSR 编号，`SystemBuffer+0x04` 为写入值 | 执行 `wrmsr`，`Information = 0`。 |
| `sub_118E0` | `SystemBuffer+0x00` 为端口号 | 结果写回 `SystemBuffer+0x00`。 |
| `sub_119A0` | `SystemBuffer+0x00` 为端口号，`SystemBuffer+0x04` 为写入值 | 按 IOCTL 选择 1、2、4 字节写端口。 |
| `sub_11E40` | `InputBufferLength == 0x10` | `+0x00` 为物理地址，`+0x08` 为元素宽度，`+0x0C` 为元素数量。 |
| `sub_11F80` | `InputBufferLength >= 0x10` 且不小于 `元素宽度 * 元素数量 + 0x10` | `+0x10` 开始是写入物理地址的数据。 |
| `sub_11BA0` | `InputBufferLength == 8` | `+0x00` 拆出 PCI bus/device/function，`+0x04` 为配置空间偏移。 |
| `sub_11C30` | `InputBufferLength >= 8` | `+0x08` 开始是写入 PCI 配置空间的数据。 |

`sub_11E40` 和 `sub_11F80` 都没有限制物理地址范围。`sub_11A50`、`sub_11AC0`、`sub_118E0`、`sub_119A0`、`sub_11BA0`、`sub_11C30` 也没有做权限细分，因此这个驱动暴露了 MSR、端口 I/O、物理内存和 PCI 配置空间访问能力。

**第三个：[ppa_x64.sys](https://github.com/lallouslab/vulnerable-drivers-ex/blob/main/drivers/ppa_x64.sys)**：
DriverEntry 反编译结果：

```c title="ppa_x64.sys: DriverEntry"
NTSTATUS __stdcall DriverEntry(PDRIVER_OBJECT DriverObject, PUNICODE_STRING RegistryPath)
{
  PDEVICE_OBJECT DeviceObject; // [rsp+40h] [rbp-38h] BYREF
  struct _UNICODE_STRING SymbolicLinkName; // [rsp+48h] [rbp-30h] BYREF
  struct _UNICODE_STRING DestinationString; // [rsp+58h] [rbp-20h] BYREF
  int v6; // [rsp+68h] [rbp-10h]

  DeviceObject = 0;
  qword_13210 = 0;
  P = ExAllocatePool(NonPagedPool, 0x50u);
  if ( !P )
    return -1073741670;
  memset(P, 0, 0x50u);
  RtlInitUnicodeString(&DestinationString, SourceString);// "\\Device\\PhyMem"
  v6 = IoCreateDevice(DriverObject, 0, &DestinationString, 0x8000u, 0, 1u, &DeviceObject);
  if ( v6 >= 0 )
  {
    DriverObject->MajorFunction[0] = (PDRIVER_DISPATCH)sub_11310;
    DriverObject->MajorFunction[2] = (PDRIVER_DISPATCH)sub_11360;
    DriverObject->MajorFunction[14] = (PDRIVER_DISPATCH)sub_113B0;
    DriverObject->DriverUnload = (PDRIVER_UNLOAD)sub_11AB0;
    RtlInitUnicodeString(&SymbolicLinkName, aDosdevicesPhym);// "\\DosDevices\\PhyMem"
    v6 = IoCreateSymbolicLink(&SymbolicLinkName, &DestinationString);
    if ( v6 < 0 )
      IoDeleteDevice(DeviceObject);
  }
  return v6;
}
```

这个驱动把 `IRP_MJ_CREATE`、`IRP_MJ_CLOSE`、`IRP_MJ_DEVICE_CONTROL` 分给了三个函数。先看 `IRP_MJ_CREATE`：

```c title="ppa_x64.sys: sub_11310"
__int64 __fastcall sub_11310(__int64 a1, IRP *Irp)
{
  Irp->IoStatus.Status = 0;
  Irp->IoStatus.Information = 0;
  IofCompleteRequest(Irp, 0);
  return 0;
}
```

`sub_11310` 只把状态置为成功并完成 IRP。`IRP_MJ_CLOSE` 对应的 `sub_11360` 逻辑相同。

继续分析 `sub_113B0`。这个函数处理 `IRP_MJ_DEVICE_CONTROL`，这里保留完整反编译代码：

```c title="ppa_x64.sys: sub_113B0"
__int64 __fastcall sub_113B0(__int64 a1, __int64 Irp)
{
  __int64 v2; // r8
  _DWORD *Event; // [rsp+48h] [rbp-70h]
  int n16; // [rsp+54h] [rbp-64h]
  PHYSICAL_ADDRESS *p_PhysicalAddress; // [rsp+58h] [rbp-60h]
  unsigned int v7; // [rsp+60h] [rbp-58h]
  DWORD n8; // [rsp+64h] [rbp-54h]
  PVOID VirtualAddress; // [rsp+70h] [rbp-48h]
  PVOID QuadPart; // [rsp+78h] [rbp-40h]
  struct _MDL *MemoryDescriptorList; // [rsp+80h] [rbp-38h]
  _QWORD *Pool; // [rsp+88h] [rbp-30h]
  _QWORD *P_1; // [rsp+90h] [rbp-28h]
  __int64 P; // [rsp+A0h] [rbp-18h]

  *(_DWORD *)(Irp + 48) = 0;
  *(_QWORD *)(Irp + 56) = 0;
  Event = CAMSchedule::GetEvent((CAMSchedule *)Irp);
  p_PhysicalAddress = *(PHYSICAL_ADDRESS **)(Irp + 24);
  n16 = Event[4];
  n8 = Event[2];
  if ( *(_BYTE *)Event == 14 )
  {
    switch ( Event[6] )
    {
      case 0x80002000:
        if ( n16 == 16 && n8 == 8 )
        {
          VirtualAddress = MmMapIoSpace(*p_PhysicalAddress, p_PhysicalAddress[1].LowPart, MmNonCached);
          if ( VirtualAddress )
          {
            MemoryDescriptorList = IoAllocateMdl(VirtualAddress, p_PhysicalAddress[1].LowPart, 0, 0, 0);
            if ( MemoryDescriptorList )
            {
              MmBuildMdlForNonPagedPool(MemoryDescriptorList);
              QuadPart = MmMapLockedPages(MemoryDescriptorList, 1);
              Pool = ExAllocatePool(NonPagedPool, 0x28u);
              Pool[1] = MemoryDescriptorList;
              Pool[2] = VirtualAddress;
              Pool[3] = QuadPart;
              *((_DWORD *)Pool + 8) = p_PhysicalAddress[1].LowPart;
              sub_11E60(&P_0, Pool);
              sub_11170(
                "Map physical 0x%x to virtual 0x%x, size %u",
                p_PhysicalAddress->QuadPart,
                (_DWORD)QuadPart,
                p_PhysicalAddress[1].LowPart);
              p_PhysicalAddress->QuadPart = (LONGLONG)QuadPart;
              *(_QWORD *)(Irp + 56) = 8;
            }
            else
            {
              MmUnmapIoSpace(VirtualAddress, p_PhysicalAddress[1].LowPart);
              *(_DWORD *)(Irp + 48) = -1073741670;
            }
          }
          else
          {
            *(_DWORD *)(Irp + 48) = -1073741670;
          }
        }
        else
        {
          *(_DWORD *)(Irp + 48) = -1073741811;
        }
        break;
      case 0x80002004:
        if ( n16 == 16 )
        {
          P = P_0;
          P_1 = (_QWORD *)P_0;
          while ( P )
          {
            if ( *(_QWORD *)(P + 24) == p_PhysicalAddress->QuadPart )
            {
              if ( *(_DWORD *)(P + 32) == p_PhysicalAddress[1].LowPart )
              {
                MmUnmapLockedPages(*(PVOID *)(P + 24), *(PMDL *)(P + 8));
                IoFreeMdl(*(PMDL *)(P + 8));
                MmUnmapIoSpace(*(PVOID *)(P + 16), *(unsigned int *)(P + 32));
                sub_11170("Unmap virtual address 0x%x, size %u", *(_QWORD *)(P + 24), *(_DWORD *)(P + 32));
                if ( P == P_0 )
                  P_0 = *(_QWORD *)P;
                else
                  *P_1 = *(_QWORD *)P;
                ExFreePoolWithTag((PVOID)P, 0);
              }
              else
              {
                *(_DWORD *)(Irp + 48) = -1073741811;
              }
              goto LABEL_54;
            }
            P_1 = (_QWORD *)P;
            P = *(_QWORD *)P;
          }
        }
        else
        {
          *(_DWORD *)(Irp + 48) = -1073741811;
        }
        break;
      case 0x80002008:
        if ( n16 == 12 && n8 == 4 )
        {
          *(_QWORD *)(Irp + 56) = 4;
          switch ( p_PhysicalAddress->HighPart )
          {
            case 1:
              p_PhysicalAddress->LowPart = (unsigned __int8)sub_11E90(p_PhysicalAddress->LowPart);
              break;
            case 2:
              p_PhysicalAddress->LowPart = (unsigned __int16)sub_11EB0(p_PhysicalAddress->LowPart);
              break;
            case 4:
              p_PhysicalAddress->LowPart = sub_11ED0(p_PhysicalAddress->LowPart);
              break;
            default:
              *(_DWORD *)(Irp + 48) = -1073741811;
              break;
          }
        }
        else
        {
          *(_DWORD *)(Irp + 48) = -1073741811;
        }
        break;
      case 0x8000200C:
        if ( n16 == 12 )
        {
          switch ( p_PhysicalAddress->HighPart )
          {
            case 1:
              sub_11EF0(p_PhysicalAddress->LowPart, LOBYTE(p_PhysicalAddress[1].LowPart));
              break;
            case 2:
              sub_11F10(p_PhysicalAddress->LowPart, LOWORD(p_PhysicalAddress[1].LowPart));
              break;
            case 4:
              sub_11F30(p_PhysicalAddress->LowPart, p_PhysicalAddress[1].LowPart);
              break;
            default:
              *(_DWORD *)(Irp + 48) = -1073741811;
              break;
          }
        }
        else
        {
          *(_DWORD *)(Irp + 48) = -1073741811;
        }
        break;
      case 0x80002012:
        if ( n16 == 20
          && p_PhysicalAddress[2].LowPart + p_PhysicalAddress[1].HighPart <= 0x100
          && n8 >= p_PhysicalAddress[2].LowPart )
        {
          LOBYTE(v2) = 1;
          *(_DWORD *)(Irp + 48) = sub_11BC0(Irp, p_PhysicalAddress, v2);
          if ( *(int *)(Irp + 48) >= 0 )
            *(_QWORD *)(Irp + 56) = p_PhysicalAddress[2].LowPart;
        }
        else
        {
          *(_DWORD *)(Irp + 48) = -1073741811;
        }
        break;
      case 0x80002016:
        if ( n16 == 20
          && p_PhysicalAddress[2].LowPart + p_PhysicalAddress[1].HighPart <= 0x100
          && n8 >= p_PhysicalAddress[2].LowPart )
        {
          *(_DWORD *)(Irp + 48) = sub_11BC0(Irp, p_PhysicalAddress, 0);
          if ( *(int *)(Irp + 48) >= 0 )
            *(_QWORD *)(Irp + 56) = p_PhysicalAddress[2].LowPart;
        }
        else
        {
          *(_DWORD *)(Irp + 48) = -1073741811;
        }
        break;
      default:
        break;
    }
  }
LABEL_54:
  v7 = *(_DWORD *)(Irp + 48);
  IofCompleteRequest((PIRP)Irp, 0);
  return v7;
}
```

有效分支如下：

| IOCTL | Method | Function | 行为 |
|---:|---|---:|---|
| `0x80002000` | `METHOD_BUFFERED` | `0x800` | 调用 `MmMapIoSpace` 映射物理地址，再用 `MmMapLockedPages` 映射到用户态。 |
| `0x80002004` | `METHOD_BUFFERED` | `0x801` | 根据用户态映射地址和长度查找链表 `P_0`，释放映射。 |
| `0x80002008` | `METHOD_BUFFERED` | `0x802` | 端口读取，支持 1、2、4 字节。 |
| `0x8000200C` | `METHOD_BUFFERED` | `0x803` | 端口写入，支持 1、2、4 字节。 |
| `0x80002012` | `METHOD_OUT_DIRECT` | `0x804` | 通过 PCI bus filter interface 读取 PCI 配置空间。 |
| `0x80002016` | `METHOD_OUT_DIRECT` | `0x805` | 通过 PCI bus filter interface 写入 PCI 配置空间。 |

`0x80002000` 要求 `InputBufferLength == 0x10`、`OutputBufferLength == 8`。`SystemBuffer` 结构按访问偏移整理如下：

| 偏移 | 含义 |
|---:|---|
| `+0x00` | 传给 `MmMapIoSpace` 的物理地址，长度 8 字节。 |
| `+0x08` | 映射长度，长度 4 字节。 |

映射成功后，驱动会分配一条 `0x28` 字节记录插入全局链表 `P_0`：

| 偏移 | 内容 |
|---:|---|
| `0x00` | 下一条记录指针。 |
| `0x08` | `IoAllocateMdl` 返回的 MDL。 |
| `0x10` | `MmMapIoSpace` 返回的内核地址。 |
| `0x18` | `MmMapLockedPages` 返回的用户态地址。 |
| `0x20` | 映射长度。 |

返回给调用方的是 `SystemBuffer+0x00` 写回的用户态地址，`IoStatus.Information = 8`。这里的物理地址和长度都来自用户态，代码没有限制物理地址范围；另外 `MmMapLockedPages` 和第二次 `ExAllocatePool` 的返回值没有判空。

`0x80002004` 要求 `InputBufferLength == 0x10`，使用 `SystemBuffer+0x00` 的用户态地址和 `SystemBuffer+0x08` 的长度匹配链表记录。匹配成功后依次调用：

1. `MmUnmapLockedPages`
2. `IoFreeMdl`
3. `MmUnmapIoSpace`
4. `ExFreePoolWithTag`

端口 I/O 的两个分支结构相同：

| IOCTL | 输入要求 | 关键字段 | 底层函数 |
|---:|---|---|---|
| `0x80002008` | `InputBufferLength == 0x0C`、`OutputBufferLength == 4` | `+0x00` 为端口号，`+0x04` 为宽度。宽度只接受 `1`、`2`、`4`。 | `sub_11E90` / `sub_11EB0` / `sub_11ED0`，对应 `in al/ax/eax, dx`。 |
| `0x8000200C` | `InputBufferLength == 0x0C` | `+0x00` 为端口号，`+0x04` 为宽度，`+0x08` 为写入值。宽度只接受 `1`、`2`、`4`。 | `sub_11EF0` / `sub_11F10` / `sub_11F30`，对应 `out dx, al/ax/eax`。 |

PCI 配置空间访问走 `sub_11BC0`。它先调用 `sub_11D90` 获取 `\\Device\\PhyMemPCIFilter` 对象，再通过 `sub_11000` 获取 PCI bus driver interface。相关全局对象如下：

| 全局符号 | 地址 | 用途 |
|---|---:|---|
| `P` | `0x13208` | `DriverEntry` 分配的 `0x50` 字节缓冲区，用于保存 PCI bus driver interface。 |
| `P_0` | `0x13210` | 物理内存映射记录链表头。 |
| `FileObject` | `0x131F8` | `IoGetDeviceObjectPointer` 输出。 |
| `DeviceObject` | `0x13200` | `IoGetDeviceObjectPointer` 输出。 |

PCI 读写 IOCTL 都要求 `InputBufferLength == 0x14`，并检查 `SystemBuffer+0x0C + SystemBuffer+0x10 <= 0x100`，同时要求 `OutputBufferLength >= *(DWORD *)(SystemBuffer+0x10)`。控制结构如下：

| 偏移 | 含义 |
|---:|---|
| `+0x00` | 传给 PCI bus interface 的 bus number，代码取低 8 位。 |
| `+0x04` | device number，代码取低 5 位。 |
| `+0x08` | function number，代码取低 3 位后左移 5 位。 |
| `+0x0C` | PCI 配置空间 offset。 |
| `+0x10` | 读写长度。 |

`0x80002012` 调用 `P+0x20` 处的函数指针读取 PCI 配置空间；`0x80002016` 调用 `P+0x28` 处的函数指针写入 PCI 配置空间。数据缓冲区来自 IRP 的 MDL：如果 MDL 标志满足 `(*(_WORD *)(Mdl + 0x0A) & 5) != 0`，直接使用 `Mdl+0x18` 指向的地址，否则调用 `MmMapLockedPagesSpecifyCache` 映射。

卸载函数 `sub_11AB0` 会循环弹出 `P_0` 链表并释放所有映射，然后在 `P` 和 `P+0x18` 非空时调用 `P+0x18` 处的函数指针，最后删除 `\\DosDevices\\PhyMem` 和设备对象。

最后整理 `ppa_x64.sys` 的攻击面：

| 位置 | 结论 |
|---|---|
| 设备名 | `\\Device\\PhyMem` |
| 符号链接 | `\\DosDevices\\PhyMem` |
| 设备类型 | `0x8000` |
| 物理内存访问 | `0x80002000` 映射物理地址，`0x80002004` 释放映射。 |
| 端口 I/O | `0x80002008` 读端口，`0x8000200C` 写端口。 |
| PCI 配置空间访问 | `0x80002012` 读，`0x80002016` 写。 |
| 主要风险 | 用户态可以映射指定物理地址、执行端口 I/O、读写 PCI 配置空间。 |

**第四个：[SysInfoX64.sys](https://github.com/lallouslab/vulnerable-drivers-ex/blob/main/drivers/SysInfoX64.sys)**：
DriverEntry 反编译结果：

```c title="SysInfoX64.sys: DriverEntry"
NTSTATUS __stdcall DriverEntry(_DRIVER_OBJECT *DriverObject, PUNICODE_STRING RegistryPath)
{
  int v3; // ebx
  int v4; // eax
  _QWORD *DeviceExtension; // rcx
  _QWORD *v6; // rcx
  __int64 n16; // rax
  struct _UNICODE_STRING DestinationString; // [rsp+40h] [rbp-28h] BYREF
  struct _UNICODE_STRING SymbolicLinkName; // [rsp+50h] [rbp-18h] BYREF
  PDEVICE_OBJECT DeviceObject; // [rsp+70h] [rbp+8h] BYREF

  DriverObject->MajorFunction[0] = (PDRIVER_DISPATCH)&sub_112DC;
  DriverObject->MajorFunction[2] = (PDRIVER_DISPATCH)&sub_112DC;
  DriverObject->MajorFunction[14] = (PDRIVER_DISPATCH)&sub_112DC;
  DriverObject->DriverStartIo = 0;
  DriverObject->DriverUnload = (PDRIVER_UNLOAD)sub_11B58;
  RtlInitUnicodeString(&DestinationString, L"\\Device\\CrystalSysInfo");
  v3 = IoCreateDevice(DriverObject, 0x390u, &DestinationString, 0x9C40u, 0, 0, &DeviceObject);
  if ( v3 >= 0 )
  {
    memset(DeviceObject->DeviceExtension, 0, 0x390u);
    RtlInitUnicodeString(&SymbolicLinkName, L"\\DosDevices\\CrystalSysInfo");
    v3 = IoCreateSymbolicLink(&SymbolicLinkName, &DestinationString);
    if ( v3 < 0 )
      IoDeleteDevice(DeviceObject);
  }
  v4 = -1;
  if ( v3 >= 0 )
    v4 = 0;
  dword_13190 = v4;
  DeviceExtension = DeviceObject->DeviceExtension;
  DeviceExtension[1] = DeviceObject;
  *(_DWORD *)DeviceExtension = 40000;
  v6 = DeviceExtension + 7;
  n16 = 16;
  do
  {
    *((_BYTE *)v6 - 32) = 0;
    *v6 = 0;
    v6 += 7;
    --n16;
  }
  while ( n16 );
  KeInitializeDpc(&DeviceObject->Dpc, DeferredRoutine, DeviceObject);
  return v3;
}
```

继续看 `sub_112DC`。这个函数分支较多，这里保留完整反编译代码用于对照；阅读分析以代码块后的表格为主：

```c title="SysInfoX64.sys: sub_112DC"
__int64 __fastcall sub_112DC(__int64 a1, IRP *Irp)
{
  struct _IO_STACK_LOCATION *CurrentStackLocation; // r11
  _DWORD *p_Information; // rsi
  __int64 v5; // rcx
  unsigned int Status; // ebx
  unsigned int n2; // ebp
  unsigned int LowPart; // r10d
  ULONG Length; // r12d
  ULONG *MasterIrp_3; // r9
  ULONG BusDataByOffset; // eax
  unsigned int Status_2; // eax
  struct _IRP *MasterIrp_2; // r8
  int v14; // eax
  unsigned __int32 v15; // eax
  CSHORT Type; // ax
  unsigned __int8 v17; // al
  unsigned int Status_1; // eax
  unsigned int Options; // ebx
  struct _IRP *MasterIrp; // r9
  struct _IRP *MasterIrp_1; // rdx
  int v22; // ecx
  int v23; // eax

  CurrentStackLocation = Irp->Tail.Overlay.CurrentStackLocation;
  p_Information = &Irp->IoStatus.Information;
  Irp->IoStatus.Information = 0;
  v5 = *(_QWORD *)(a1 + 64);
  Status = -1073741822;
  if ( !CurrentStackLocation->MajorFunction )
  {
    if ( dword_13190 == -1 )
      goto LABEL_70;
    v23 = dword_13190 + 1;
    goto LABEL_69;
  }
  n2 = 2;
  if ( CurrentStackLocation->MajorFunction == 2 )
  {
    if ( dword_13190 == -1 )
      goto LABEL_70;
    v23 = dword_13190 - 1;
LABEL_69:
    dword_13190 = v23;
    goto LABEL_70;
  }
  if ( CurrentStackLocation->MajorFunction != 14 )
    goto LABEL_71;
  LowPart = CurrentStackLocation->Parameters.Read.ByteOffset.LowPart;
  if ( LowPart > 0x9C40A440 )
  {
    if ( LowPart != -1673485244 && LowPart != -1673485240 )
    {
      switch ( LowPart )
      {
        case 0x9C40A4C0:
          Status_2 = sub_11860(
                       Irp->AssociatedIrp.MasterIrp,
                       CurrentStackLocation->Parameters.Create.Options,
                       Irp->AssociatedIrp.MasterIrp,
                       CurrentStackLocation->Parameters.Read.Length,
                       (__int64)p_Information);
          break;
        case 0x9C40A540:
          Options = CurrentStackLocation->Parameters.Create.Options;
          if ( Options >= 8 )
          {
            MasterIrp = Irp->AssociatedIrp.MasterIrp;
            *p_Information = 0;
            Status = Options - 8 != HalSetBusDataByOffset(
                                      PCIConfiguration,
                                      (unsigned __int8)BYTE1(*(_DWORD *)&MasterIrp->Type),
                                      (32 * (*(_DWORD *)&MasterIrp->Type & 7))
                                    | ((unsigned __int8)*(_DWORD *)&MasterIrp->Type >> 3),
                                      &MasterIrp->MdlAddress,
                                      *(_DWORD *)(&MasterIrp->Size + 1),
                                      Options - 8)
                   ? 0xE0000003
                   : 0;
            goto LABEL_71;
          }
          goto LABEL_59;
        case 0x9C40A580:
          if ( CurrentStackLocation->Parameters.Create.Options == 4 )
          {
            Status_1 = sub_11BE8(v5, *(unsigned int *)Irp->AssociatedIrp.MasterIrp, 2621482052LL);
            *(_QWORD *)p_Information = 0;
            Status = Status_1;
            goto LABEL_71;
          }
          goto LABEL_59;
        case 0x9C40A584:
          goto LABEL_70;
        case 0x9C40A5C0:
          Status_2 = sub_11938(
                       Irp->AssociatedIrp.MasterIrp,
                       CurrentStackLocation->Parameters.Create.Options,
                       Irp->AssociatedIrp.MasterIrp,
                       CurrentStackLocation->Parameters.Read.Length,
                       (__int64)p_Information);
          break;
        case 0x9C40A600:
          Status_2 = sub_11A6C(
                       Irp->AssociatedIrp.MasterIrp,
                       CurrentStackLocation->Parameters.Create.Options,
                       Irp->AssociatedIrp.MasterIrp,
                       CurrentStackLocation->Parameters.Read.Length,
                       (__int64)p_Information);
          break;
        default:
          goto LABEL_71;
      }
LABEL_39:
      Status = Status_2;
      goto LABEL_71;
    }
LABEL_55:
    MasterIrp_1 = Irp->AssociatedIrp.MasterIrp;
    if ( LowPart != -1673485244 )
    {
      n2 = 4;
      if ( LowPart != -1673485240 )
        n2 = 1;
    }
    if ( CurrentStackLocation->Parameters.Create.Options >= (unsigned __int64)n2 + 4 )
    {
      v22 = *(_DWORD *)&MasterIrp_1->Type;
      if ( LowPart == -1673485248 )
      {
        __outbyte(v22, *((_BYTE *)&MasterIrp_1->Size + 2));
      }
      else if ( LowPart == -1673485244 )
      {
        __outword(v22, *(&MasterIrp_1->Size + 1));
      }
      else
      {
        __outdword(v22, *(_DWORD *)(&MasterIrp_1->Size + 1));
      }
      goto LABEL_70;
    }
    goto LABEL_59;
  }
  switch ( LowPart )
  {
    case 0x9C40A440:
      goto LABEL_55;
    case 0x9C402604:
      Status_2 = sub_11708(
                   -1673517564,
                   Irp->AssociatedIrp.MasterIrp,
                   CurrentStackLocation->Parameters.Create.Options,
                   Irp->AssociatedIrp.MasterIrp,
                   CurrentStackLocation->Parameters.Read.Length,
                   (__int64)p_Information);
      goto LABEL_39;
    case 0x9C402608:
      Status_2 = sub_11748(
                   -1673517560,
                   Irp->AssociatedIrp.MasterIrp,
                   CurrentStackLocation->Parameters.Create.Options,
                   Irp->AssociatedIrp.MasterIrp,
                   CurrentStackLocation->Parameters.Read.Length,
                   (__int64)p_Information);
      goto LABEL_39;
    case 0x9C402664:
      *(_DWORD *)Irp->AssociatedIrp.MasterIrp = dword_13190;
      *(_QWORD *)p_Information = 4;
      goto LABEL_70;
    case 0x9C406400:
    case 0x9C406404:
    case 0x9C406408:
      MasterIrp_2 = Irp->AssociatedIrp.MasterIrp;
      if ( LowPart != -1673501692 )
      {
        n2 = 4;
        if ( LowPart != -1673501688 )
          n2 = 1;
      }
      if ( CurrentStackLocation->Parameters.Create.Options == 4 && CurrentStackLocation->Parameters.Read.Length >= n2 )
      {
        v14 = *(_DWORD *)&MasterIrp_2->Type;
        if ( LowPart == -1673501696 )
        {
          v17 = __inbyte(v14);
          LOBYTE(MasterIrp_2->Type) = v17;
        }
        else if ( LowPart == -1673501692 )
        {
          Type = __inword(v14);
          MasterIrp_2->Type = Type;
        }
        else
        {
          v15 = __indword(v14);
          *(_DWORD *)&MasterIrp_2->Type = v15;
        }
        *p_Information = n2;
        goto LABEL_70;
      }
      goto LABEL_59;
    case 0x9C406480:
      Status_2 = sub_11784(
                   Irp->AssociatedIrp.MasterIrp,
                   CurrentStackLocation->Parameters.Create.Options,
                   Irp->AssociatedIrp.MasterIrp,
                   CurrentStackLocation->Parameters.Read.Length,
                   (__int64)p_Information);
      goto LABEL_39;
    case 0x9C406500:
      Length = CurrentStackLocation->Parameters.Read.Length;
      if ( CurrentStackLocation->Parameters.Create.Options == 8 )
      {
        MasterIrp_3 = (ULONG *)Irp->AssociatedIrp.MasterIrp;
        BusDataByOffset = HalGetBusDataByOffset(
                            PCIConfiguration,
                            (unsigned __int8)BYTE1(*MasterIrp_3),
                            (32 * (*MasterIrp_3 & 7)) | ((unsigned __int8)*MasterIrp_3 >> 3),
                            MasterIrp_3,
                            MasterIrp_3[1],
                            CurrentStackLocation->Parameters.Read.Length);
        if ( !BusDataByOffset )
        {
          Status = -536870911;
LABEL_22:
          *p_Information = 0;
          break;
        }
        if ( Length != 2 && BusDataByOffset == 2 )
        {
          Status = -536870910;
          goto LABEL_22;
        }
        if ( Length != BusDataByOffset )
        {
          Status = -536870908;
          goto LABEL_22;
        }
        *p_Information = Length;
LABEL_70:
        Status = 0;
        break;
      }
LABEL_59:
      Status = -1073741811;
      break;
  }
LABEL_71:
  Irp->IoStatus.Status = Status;
  IofCompleteRequest(Irp, 0);
  return Status;
}
```

整理如下：

| 位置 | 值 |
|---|---|
| 设备名 | `\\Device\\CrystalSysInfo` |
| 符号链接 | `\\DosDevices\\CrystalSysInfo` |
| 设备类型 | `0x9C40` |
| 设备扩展大小 | `0x390` |
| 派发函数 | `sub_112DC` |
| 卸载函数 | `sub_11B58` |
| 全局计数 | `dword_13190`，地址 `0x13190` |

`DriverEntry` 会初始化 16 个中断相关槽位，每个槽位大小为 `0x38`。`off_13110` 里有 16 个 ISR 表项，从 `sub_11074` 到 `sub_1119C`，都跳到 `sub_11008`，区别只是传入的编号为 `0` 到 `15`。`sub_11008` 会断开对应槽位里的 `InterruptObject`，然后调用 `KeInsertQueueDpc`。

`sub_112DC` 的有效 IOCTL 如下：

| IOCTL | Function | handler | 行为 |
|---:|---:|---|---|
| `0x9C402604` | `0x981` | `sub_11708` | `rdmsr`。 |
| `0x9C402608` | `0x982` | `sub_11748` | `wrmsr`。 |
| `0x9C402664` | `0x999` | `sub_112DC` 内联处理 | 写回 `dword_13190`。 |
| `0x9C406400` | `0x900` | `sub_112DC` 内联处理 | `in al, dx`，读 1 字节端口。 |
| `0x9C406404` | `0x901` | `sub_112DC` 内联处理 | `in ax, dx`，读 2 字节端口。 |
| `0x9C406408` | `0x902` | `sub_112DC` 内联处理 | `in eax, dx`，读 4 字节端口。 |
| `0x9C406480` | `0x920` | `sub_11784` | `MmMapIoSpace` 后读物理地址内容。 |
| `0x9C406500` | `0x940` | `sub_112DC` 内联处理 | `HalGetBusDataByOffset` 读 PCI 配置空间。 |
| `0x9C40A440` | `0x910` | `sub_112DC` 内联处理 | `out dx, al`，写 1 字节端口。 |
| `0x9C40A444` | `0x911` | `sub_112DC` 内联处理 | `out dx, ax`，写 2 字节端口。 |
| `0x9C40A448` | `0x912` | `sub_112DC` 内联处理 | `out dx, eax`，写 4 字节端口。 |
| `0x9C40A4C0` | `0x930` | `sub_11860` | `MmMapIoSpace` 后写物理地址内容。 |
| `0x9C40A540` | `0x950` | `sub_112DC` 内联处理 | `HalSetBusDataByOffset` 写 PCI 配置空间。 |
| `0x9C40A580` | `0x960` | `sub_11BE8` | 按输入编号连接中断。 |
| `0x9C40A584` | `0x961` | `sub_112DC` 内联处理 | 返回成功。 |
| `0x9C40A5C0` | `0x970` | `sub_11938` | 两段物理地址都 `MmMapIoSpace` 后复制。 |
| `0x9C40A600` | `0x980` | `sub_11A6C` | `MmMapIoSpace` 后用同一个值填充物理地址范围。 |

几个关键缓冲区格式：

| handler / IOCTL | 输入要求 | 关键字段 |
|---|---|---|
| `sub_11708` | 读取 `SystemBuffer+0x00` 作为 MSR 编号 | 结果写回 `SystemBuffer+0x00`，`Information = 8`。 |
| `sub_11748` | `SystemBuffer+0x00` 为 MSR 编号，`+0x04` 和 `+0x08` 拼成写入值 | 执行 `wrmsr`，`Information = 0`。 |
| 端口读 | `InputBufferLength == 4`，`OutputBufferLength >= 1/2/4` | `SystemBuffer+0x00` 为端口号。 |
| 端口写 | `InputBufferLength >= 5/6/8` | `SystemBuffer+0x00` 为端口号，`+0x04` 为写入值。 |
| `sub_11784` | `InputBufferLength == 0x0C` | `+0x00` 为物理地址，`+0x04` 为元素宽度，`+0x08` 为元素数量。 |
| `sub_11860` | `InputBufferLength >= 元素宽度 * 元素数量 + 0x0C` | `+0x00` 为物理地址，`+0x04` 为元素宽度，`+0x08` 为元素数量，`+0x0C` 起为写入数据。 |
| `sub_11938` | `InputBufferLength >= 0x10` | `+0x00` 为源物理地址，`+0x04` 为目标物理地址，`+0x08` 为元素宽度，`+0x0C` 为元素数量。 |
| `sub_11A6C` | `InputBufferLength >= 0x10` | `+0x00` 为物理地址，`+0x04` 为填充值，`+0x08` 为元素宽度，`+0x0C` 为元素数量。 |
| PCI 读 | `InputBufferLength == 8` | `+0x00` 拆出 bus/device/function，`+0x04` 为配置空间偏移。 |
| PCI 写 | `InputBufferLength >= 8` | `+0x00` 拆出 bus/device/function，`+0x04` 为配置空间偏移，`+0x08` 起为写入数据。 |
| `sub_11BE8` | `InputBufferLength == 4` | `SystemBuffer+0x00` 为中断槽位编号。 |

卸载函数 `sub_11B58` 会删除 `\\DosDevices\\CrystalSysInfo`，遍历 16 个槽位断开非空 `InterruptObject`，关闭非空 `Handle`，最后调用 `IoDeleteDevice`。

最后整理 `SysInfoX64.sys` 的攻击面：

| 位置 | 结论 |
|---|---|
| 设备名 | `\\Device\\CrystalSysInfo` |
| 符号链接 | `\\DosDevices\\CrystalSysInfo` |
| 设备类型 | `0x9C40` |
| MSR 访问 | `0x9C402604` 读 MSR，`0x9C402608` 写 MSR。 |
| 端口 I/O | `0x9C406400`、`0x9C406404`、`0x9C406408` 读端口；`0x9C40A440`、`0x9C40A444`、`0x9C40A448` 写端口。 |
| 物理内存访问 | `0x9C406480` 读，`0x9C40A4C0` 写，`0x9C40A5C0` 复制，`0x9C40A600` 填充。 |
| PCI 配置空间访问 | `0x9C406500` 读，`0x9C40A540` 写。 |
| 中断相关能力 | `0x9C40A580` 连接中断，卸载时和 ISR 路径会断开中断。 |
| 主要风险 | 用户态可以触发 MSR、端口 I/O、物理内存、PCI 配置空间和中断连接相关操作。 |

参考文档：

- [IRP major function codes](https://learn.microsoft.com/en-us/windows-hardware/drivers/kernel/irp-major-function-codes)
- [IRP_MJ_CREATE](https://learn.microsoft.com/en-us/windows-hardware/drivers/kernel/irp-mj-create)
- [IRP_MJ_CLOSE](https://learn.microsoft.com/en-us/windows-hardware/drivers/kernel/irp-mj-close)
- [IRP_MJ_DEVICE_CONTROL](https://learn.microsoft.com/en-us/windows-hardware/drivers/kernel/irp-mj-device-control)
